import BookACallClient from "./BookACallClient";

export const metadata = {
  title: "Book a Call | Miami Home Guide",
  description: "Leave your info and a licensed Miami real estate agent will reach out to set up a time to talk — no obligation.",
};

export default function Page() {
  return <BookACallClient />;
}
